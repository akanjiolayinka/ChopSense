"""Persona extraction node.

Turns raw conversational input into a structured persona — location, group,
budget, mood, occasion, dietary signals. This is the layer that decides what
the user actually wants before any retrieval happens.
"""


def understand(state):
    # TODO: call the LLM with the persona-extraction prompt and update state.
    return state
