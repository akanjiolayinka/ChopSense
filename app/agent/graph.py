"""LangGraph state machine that orchestrates the recommendation pipeline.

The graph wires together understand → retrieve → rerank → reason → nigerianize
and exposes a single compiled object the API layer can invoke.
"""

# from langgraph.graph import StateGraph, END


# TODO: build the StateGraph, register each node, define edges, and compile.
